[
  {
    "name": "add-fa14892031114146",
    "description": "add two numbers",
    "inputSchema": {
      "properties": {
        "a": {
          "description": "first number",
          "type": "number"
        },
        "b": {
          "description": "second number",
          "type": "number"
        }
      },
      "required": [
        "a",
        "b"
      ],
      "type": "object"
    }
  },
  {
    "name": "multiply-fc1c02d5f80530d0",
    "description": "multiply two numbers",
    "inputSchema": {
      "properties": {
        "a": {
          "description": "first number",
          "type": "number"
        },
        "b": {
          "description": "second number",
          "type": "number"
        }
      },
      "required": [
        "a",
        "b"
      ],
      "type": "object"
    }
  },
  {
    "name": "time-dc8e822ec8e9b3a4",
    "description": "get current date and time",
    "inputSchema": {
      "properties": {
        "format": {
          "description": "time format: iso / date / time / unix",
          "enum": [
            "iso",
            "date",
            "time",
            "unix"
          ],
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "uuid-674729dbad0d1817",
    "description": "generate a UUID (default v7, time-ordered)",
    "inputSchema": {
      "properties": {
        "count": {
          "default": 1,
          "description": "generation count (default 1, max 10)",
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "url_exact_search-275f8eeb7ada8534",
    "description": "Extract and convert web page content to clean, readable markdown format. Perfect for reading articles, documentation, blog posts, or any web content. Use this when you need to analyze text content from websites, bypass paywalls, or get structured data.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "url": {
          "anyOf": [
            {
              "format": "uri",
              "type": "string"
            },
            {
              "items": {
                "format": "uri",
                "type": "string"
              },
              "type": "array"
            }
          ],
          "description": "The complete URL of the webpage or PDF file to read and convert (e.g., 'https://example.com/article'). Can be a single URL string or an array of URLs for parallel reading."
        },
        "withAllImages": {
          "description": "Set to true to extract and return all images found on the page as structured data",
          "type": "boolean"
        },
        "withAllLinks": {
          "description": "Set to true to extract and return all hyperlinks found on the page as structured data",
          "type": "boolean"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "execution": {
      "taskSupport": "forbidden"
    }
  },
  {
    "name": "natural_language_search-4747bf45dde3082a",
    "description": "Search the entire web for current information, news, articles, and websites. Use this when you need up-to-date information, want to find specific websites, research topics, or get the latest news. Ideal for answering questions about recent events, finding resources, or discovering relevant content.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "gl": {
          "description": "Country code, e.g., 'dz' for Algeria",
          "type": "string"
        },
        "hl": {
          "description": "Language code, e.g., 'zh-cn' for Simplified Chinese",
          "type": "string"
        },
        "location": {
          "description": "Location for search results, e.g., 'London', 'New York', 'Tokyo'",
          "type": "string"
        },
        "num": {
          "default": 30,
          "description": "Maximum number of search results to return, between 1-100",
          "type": "number"
        },
        "query": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          ],
          "description": "Search terms or keywords to find relevant web content (e.g., 'climate change news 2024', 'best pizza recipe'). Can be a single query string or an array of queries for parallel search."
        },
        "tbs": {
          "description": "Time-based search parameter, e.g., 'qdr:h' for past hour, can be qdr:h, qdr:d, qdr:w, qdr:m, qdr:y",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "execution": {
      "taskSupport": "forbidden"
    }
  },
  {
    "name": "batch_edit-3e5e182934e3cd7a",
    "description": "Perform multiple insert/replace operations on a SINGLE file atomically.",
    "inputSchema": {
      "$defs": {
        "BatchEditLineRange": {
          "anyOf": [
            {
              "description": "Single line number for insert (0=beginning, N=after line N, -1=after last line)",
              "format": "int64",
              "type": "integer"
            },
            {
              "description": "Line range [start, end] for replace (1-indexed, inclusive)",
              "items": {
                "format": "int64",
                "type": "integer"
              },
              "type": "array"
            },
            {
              "description": "Single hash identifier for insert (hash mode: insert after line with this hash)",
              "type": "string"
            },
            {
              "description": "Hash range [start_hash, end_hash] for replace (hash mode)",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          ]
        },
        "BatchEditOperation": {
          "properties": {
            "content": {
              "description": "Raw content to insert or replace with.",
              "type": "string"
            },
            "line_range": {
              "$ref": "#/$defs/BatchEditLineRange",
              "description": "Line numbers from ORIGINAL file content."
            },
            "operation": {
              "$ref": "#/$defs/BatchEditOperationType",
              "description": "Type of operation: 'insert' (after line) or 'replace' (line range)"
            }
          },
          "required": [
            "operation",
            "line_range",
            "content"
          ],
          "type": "object"
        },
        "BatchEditOperationType": {
          "enum": [
            "insert",
            "replace"
          ],
          "type": "string"
        }
      },
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "operations": {
          "description": "Array of operations for batch_edit on SINGLE file. Max 50 operations.",
          "items": {
            "$ref": "#/$defs/BatchEditOperation"
          },
          "maxItems": 50,
          "type": "array"
        },
        "path": {
          "description": "Path to the file to edit",
          "type": "string"
        }
      },
      "required": [
        "path",
        "operations"
      ],
      "title": "BatchEditParams",
      "type": "object"
    }
  },
  {
    "name": "extract_lines-534444f6c8376fe7",
    "description": "Copy lines from a source file and append them into a target file.",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "append_line": {
          "description": "Position where to append: 0=beginning, -1=end, N=after line N (1-indexed)",
          "format": "int64",
          "type": "integer"
        },
        "append_path": {
          "description": "Path to the target file where extracted lines will be appended",
          "type": "string"
        },
        "from_path": {
          "description": "Path to the source file to extract lines from",
          "type": "string"
        },
        "from_range": {
          "description": "Two-element array [start, end] with 1-indexed line numbers (inclusive)",
          "items": {
            "format": "int64",
            "type": "integer"
          },
          "maxItems": 2,
          "minItems": 2,
          "type": "array"
        }
      },
      "required": [
        "from_path",
        "from_range",
        "append_path",
        "append_line"
      ],
      "title": "ExtractLinesParams",
      "type": "object"
    }
  },
  {
    "name": "shell-3707130aa53ba3b6",
    "description": "Execute a command in the shell.",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "background": {
          "default": null,
          "description": "Run command in background and return PID",
          "type": [
            "boolean",
            "null"
          ]
        },
        "command": {
          "description": "The shell command to execute",
          "type": "string"
        }
      },
      "required": [
        "command"
      ],
      "title": "ShellParams",
      "type": "object"
    }
  },
  {
    "name": "text_editor-41d3bcaeb2d58e86",
    "description": "Perform text editing operations on files: create, str_replace, delete, undo_edit.",
    "inputSchema": {
      "$defs": {
        "TextEditorCommand": {
          "enum": [
            "create",
            "str_replace",
            "delete",
            "undo_edit"
          ],
          "type": "string"
        }
      },
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "command": {
          "$ref": "#/$defs/TextEditorCommand",
          "description": "The operation to perform: create, str_replace, undo_edit"
        },
        "content": {
          "default": null,
          "description": "File content for create command.",
          "type": [
            "string",
            "null"
          ]
        },
        "new_text": {
          "default": null,
          "description": "Replacement text. REQUIRED for str_replace.",
          "type": [
            "string",
            "null"
          ]
        },
        "old_text": {
          "default": null,
          "description": "Text to find (must match exactly). REQUIRED for str_replace.",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "REQUIRED. Path to the file to operate on.",
          "type": "string"
        }
      },
      "required": [
        "command",
        "path"
      ],
      "title": "TextEditorParams",
      "type": "object"
    }
  },
  {
    "name": "view-b89ae5c22a258b12",
    "description": "Read files, view directories, and search file content. Unified read-only tool.",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "content": {
          "default": null,
          "description": "Content search string. By default treated as a literal substring.\nSet `regex: true` to interpret as a Rust regex (case-insensitive via `(?i)` prefix,\ne.g. `(?i)error`). Only used when path is a directory or a single file.",
          "type": [
            "string",
            "null"
          ]
        },
        "context": {
          "default": null,
          "description": "Context lines around content search matches.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "include_hidden": {
          "default": null,
          "description": "Include hidden files/directories starting with '.'.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "lines": {
          "default": null,
          "description": "Line range for file viewing. Accepts ONE of two shapes (max 2 levels of nesting):\n\n1. Flat range: `[start, end]` — single range applied to single file or all files.\n   Example: `[1, 50]` or `[\"a3f2\", \"b8c1\"]` (hash endpoints).\n\n2. Nested ranges: `[[start, end], [start, end], ...]`\n   - With ONE path: multiple ranges from that file. Example: `[[1, 50], [200, 250]]`.\n   - With N paths: per-file ranges (one per path). Example: `[[1, 50], [10, 30]]`.\n\nEndpoints are 1-indexed line numbers (i64) or hash strings from previous `view` output.\nNEVER triple-wrap: `[[[1,50]]]` is INVALID. Maximum nesting is array-of-pairs.",
          "oneOf": [
            {
              "description": "Flat range [start, end] — applied to single file or all files",
              "examples": [
                [
                  1,
                  50
                ],
                [
                  "a3f2",
                  "b8c1"
                ]
              ],
              "items": {
                "oneOf": [
                  {
                    "format": "int64",
                    "type": "integer"
                  },
                  {
                    "type": "string"
                  }
                ]
              },
              "maxItems": 2,
              "minItems": 2,
              "type": "array"
            },
            {
              "description": "Nested [[start,end], ...] — multiple ranges per file or per-file ranges",
              "examples": [
                [
                  [
                    1,
                    50
                  ],
                  [
                    200,
                    250
                  ]
                ],
                [
                  [
                    1,
                    30
                  ],
                  [
                    10,
                    40
                  ]
                ]
              ],
              "items": {
                "items": {
                  "oneOf": [
                    {
                      "format": "int64",
                      "type": "integer"
                    },
                    {
                      "type": "string"
                    }
                  ]
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "minItems": 1,
              "type": "array"
            }
          ]
        },
        "max_depth": {
          "default": null,
          "description": "Maximum directory traversal depth.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "paths": {
          "description": "One or more file/directory paths. Single path for file viewing or directory listing; multiple paths for multi-file viewing (max 50).",
          "items": {
            "type": "string"
          },
          "maxItems": 50,
          "minItems": 1,
          "type": "array"
        },
        "pattern": {
          "default": null,
          "description": "Filename glob filter for directory listing.",
          "type": [
            "string",
            "null"
          ]
        },
        "regex": {
          "default": null,
          "description": "When true, `content` is a regex pattern instead of a literal substring. Default: false.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "required": [
        "paths"
      ],
      "title": "ViewParams",
      "type": "object"
    }
  },
  {
    "name": "workdir-2808d998c446ff46",
    "description": "Change the working directory used by subsequent tool calls. Do NOT call this just to check the current directory — all tools accept both relative and absolute paths and resolve relative paths against the session's working directory automatically. Only invoke this tool when you actually need to switch to a different directory (set `path`) or revert to the session root (`reset: true`).",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "path": {
          "default": null,
          "description": "Absolute path or path relative to current workdir to switch into.\nRequired unless `reset: true`. Do not pass `\".\"` — that is a no-op.",
          "type": [
            "string",
            "null"
          ]
        },
        "reset": {
          "default": null,
          "description": "If true, revert to the original session working directory.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "title": "WorkdirParams",
      "type": "object"
    }
  }
]